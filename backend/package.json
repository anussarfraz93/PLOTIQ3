{
  "name": "plotiq-backend",
  "version": "1.0.0",
  "private": true,
  "description": "PLOTIQ recommendation API — Express + SQLite",
  "main": "src/server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --watch --watch-path=src src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
